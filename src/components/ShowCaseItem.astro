---
// SPDX-FileCopyrightText: Simon Schneegans <code@simonschneegans.de>
// SPDX-License-Identifier: CC0-1.0

interface Props {
  type: 'left' | 'right' | 'centered' | 'fill' | 'fullwidth';
}

const { type } = Astro.props;
---

<div
  class:list={[
    'show-case-item',
    { reverse: type === 'right' },
    { centered: type === 'centered' },
    { fill: type === 'fill' },
    { fullwidth: type === 'fullwidth' },
  ]}>
  <div class="description">
    <h2>
      <slot name="title" />
    </h2>
    <slot name="description" />
  </div>
  <div class="content">
    <slot name="content" />
  </div>
</div>

<style>
  .show-case-item {
    margin-top: min(120px, calc(15vh));
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .description,
  .content {
    flex: 1 1 0;
  }

  .centered {
    text-align: center;
  }

  .fullwidth {
    width: 100vw;
    position: relative;
    margin-left: -50vw;
    margin-top: 100px;
    left: 50%;
  }

  .fill .content,
  .fullwidth .content,
  .centered .content {
    display: none;
  }

  .description {
    font-size: clamp(1rem, calc(0.5rem + 1vw), 1.5rem);
  }

  @media (min-width: 50rem) {
    .show-case-item {
      gap: 5vw;
      flex-direction: row;
    }

    .show-case-item.reverse {
      flex-direction: row-reverse;
    }
  }
</style>
